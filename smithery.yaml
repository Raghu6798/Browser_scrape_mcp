# smithery.yml – Smithery.ai configuration for a Python-based MCP

startCommand:
  type: stdio
  configSchema: {}  # Define JSON schema here if needed, otherwise leave empty

commandFunction: |-
  (config) => ({
    command: "python",
    args: [
      "main.py"
    ],
    env: {
      "MISTRAL_API_KEY": process.env.MISTRAL_API_KEY || "",
      "PYTHONUNBUFFERED": "1"
    }
  })
